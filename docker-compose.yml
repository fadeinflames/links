version: '3.8'

services:
  links-app:
    build: .
    container_name: links-app
    ports:
      - "3000:3000"
    volumes:
      # Persist database
      - ./data:/app/data
    environment:
      - PORT=3000
    restart: unless-stopped

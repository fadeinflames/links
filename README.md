# Links App - Простое приложение для ссылок со статистикой

Простое и легкое приложение для создания страницы со ссылками (аналог Linktree/Linkstack) с базовой статистикой кликов.

## Возможности

- ✨ Темный дизайн с белыми контурами
- 📊 Статистика кликов по ссылкам
- 👥 Подсчет уникальных посетителей
- 📅 История кликов по дням
- 🔗 Отслеживание всех кликов (социальные иконки и кнопки ссылок)

## Установка

### Вариант 1: Docker Compose (рекомендуется)

1. Убедитесь, что у вас установлены Docker и Docker Compose
2. Запустите приложение:
```bash
docker-compose up -d
```

3. Откройте в браузере:
```
http://localhost:3000
```

### Вариант 2: Локальная установка

1. Установите зависимости:
```bash
npm install
```

2. Запустите сервер:
```bash
npm start
```

3. Откройте в браузере:
```
http://localhost:3000
```

## Настройка

### Изменение ссылок и информации

Отредактируйте файл `public/index.html`:
- Замените URL в атрибутах `href` на ваши реальные ссылки
- Измените имя, должность в секции профиля
- Добавьте свою фотографию (замените `src` у `.profile-picture`)
- Настройте секции ссылок под себя

### Изменение порта

По умолчанию приложение работает на порту 3000. Чтобы изменить:
```bash
PORT=8080 npm start
```

## Статистика

Статистика доступна по адресу: `http://localhost:3000/stats`

Статистика включает:
- Общее количество кликов
- Клики по каждой ссылке
- Количество уникальных посетителей
- Последние 50 кликов с IP адресами
- График кликов по дням (последние 30 дней)

## Запуск с Docker Compose

Самый простой способ запустить приложение:

```bash
docker-compose up -d
```

Приложение будет доступно на `http://localhost:3000`

Остановить:
```bash
docker-compose down
```

Пересобрать после изменений:
```bash
docker-compose up -d --build
```

## База данных

Данные хранятся в SQLite базе данных в директории `data/links.db` (создается автоматически). При использовании Docker Compose данные сохраняются в volume.

## Структура проекта

```
.
├── server.js          # Express сервер и API
├── package.json       # Зависимости
├── public/
│   ├── index.html    # Главная страница со ссылками
│   ├── stats.html    # Страница статистики
│   ├── style.css     # Стили
│   └── script.js     # JavaScript для отслеживания кликов
└── README.md
```
